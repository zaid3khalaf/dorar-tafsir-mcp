[project]
name = "dorar-tafsir-mcp"
version = "2.0.0"
description = "Read-only MCP connector for Dorar Tafsir Encyclopedia"
requires-python = ">=3.10"
dependencies = [
  "mcp[cli]>=2.0,<3",
  "fastapi>=0.115",
  "uvicorn[standard]>=0.30",
  "httpx>=0.27",
  "beautifulsoup4>=4.12",
]
